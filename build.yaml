targets:
  $default:
    builders:
      riverpod_generator:
        options:
          provider_family_name: ProviderFamily
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
